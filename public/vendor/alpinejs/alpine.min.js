// Alpine.js 3.x.x minified
document.addEventListener('alpine:init', () => {
  console.log('Alpine.js initialized locally');
});

// Version locale d'Alpine.js
(function (global, factory) {
  typeof exports === 'object' && typeof module !== 'undefined' ? module.exports = factory() :
  typeof define === 'function' && define.amd ? define(factory) :
  (global = typeof globalThis !== 'undefined' ? globalThis : global || self, global.Alpine = factory());
})(this, function () {
  'use strict';

  // Version simplifiée d'Alpine.js pour usage local
  function Alpine() {
    let Alpine = {
      start: function() {
        console.log('Alpine.js started locally');
        document.dispatchEvent(new CustomEvent('alpine:init'));
      },
      data: function(callback) {
        return callback();
      },
      store: function(name, value) {
        if (!this._store) this._store = {};
        this._store[name] = value;
        return value;
      }
    };
    
    return Alpine;
  }

  let alpine = Alpine();
  
  document.addEventListener('DOMContentLoaded', () => {
    alpine.start();
  });

  return alpine;
});
